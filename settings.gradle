pluginManagement {
    repositories {
        google()
        mavenCentral()
        //maven { url 'https://jitpack.io' }
        maven {
            url "https://repo.itextsupport.com/android"
        }

        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //maven { url 'https://jitpack.io' }
        maven {
            url "https://repo.itextsupport.com/android"
        }

    }
}
rootProject.name = "zoetechv1.0"
include ':app'
